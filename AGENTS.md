1. after doing the changes, make sure to run "bun run lint", "bun run fmt" and "bun run knip" and fix the issues.
2. don't run dev server unless i tell you to.
3. use bun as the package manager, nothing else

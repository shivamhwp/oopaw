export {
  clampArray,
  createExcerpt,
  createFeedItemId,
  createSourceId,
  createStableHash,
  dedupeItems,
  getHostnameLabel,
  normalizeInputUrl,
  normalizeOptionalDate,
  resolveUrl,
  sortItemsNewestFirst,
  stripHtml,
} from "@repo/shared/feed/utils";

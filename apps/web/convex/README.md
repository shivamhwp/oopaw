# Convex linkage

This app reads generated Convex client types from the root `/convex` workspace.

- Server functions live in `/convex`.
- `apps/web/convex/_generated` is linked to `/convex/_generated`.
- Web client imports should go through `src/lib/convex.ts`.

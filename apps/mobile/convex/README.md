# Convex linkage

This app reads generated Convex client types from the root `/convex` workspace.

- Server functions live in `/convex`.
- `apps/mobile/convex/_generated` is linked to `/convex/_generated`.
- Mobile client imports should go through `src/lib/convex.ts`.
